---
last_updated: "2026-07-14"
---

# FocusBro — Requirements

> The real and true requirements this build is held accountable to. Numbered, testable, statused. Every control-issue slice references the requirement IDs it advances; every `live` flip needs verification evidence in the control-issue comment. Status ∈ `live | building | roadmap | rejected | parked`. `rejected` rows stay here with their reason — rejected scope is a guardrail.
>
> Control plane: [issue #6](https://github.com/Latimer-Woods-Tech/focusbro/issues/6). Statuses below verified against live focusbro.net on **2026-07-04 ~22:20 UTC** (browser-UA curl; CF WAF blocks bot UAs).

## Functional

| ID | Requirement | Status | Verification |
|---|---|---|---|
| R-001 | `GET /ads.txt` serves `google.com, pub-1346297152611586, DIRECT, f08c47fec0942fa0` (matches the homepage AdSense client) | building | `curl -A "<browser UA>" https://focusbro.net/ads.txt`. Fixed in source (`3b9a2e9`, both `public/ads.txt` and the Worker route) but **live still serves old `pub-7015938501859914`** — deploy pipeline silently broken (see ARCHITECTURE Known Debt) |
| R-002 | Zero placeholder ad markup: live homepage HTML contains no "Ad Space" labels and no `data-ad-slot="XXXXXXXXXX"`; the `adsbygoogle.js?client=ca-pub-1346297152611586` ownership script is kept | building | `curl -A "<browser UA>" https://focusbro.net/ \| grep -c 'Ad Space\|data-ad-slot="XXXXXXXXXX"'` → 0. Removed in source (`3b9a2e9`); **live HTML still has 3× each** (2026-07-04) |
| R-003 | No presence-faking feature anywhere: no "Keep Teams/Slack green" widget, no keystroke-companion download, no synthetic activity engine; legitimate Screen Wake Lock only | building | `curl -A "<browser UA>" https://focusbro.net/ \| grep -c 'Keep Teams\|simulateActivity\|downloadPresenceScript'` → 0. Removed in source (`3b9a2e9`); **live HTML still has the widget** (2026-07-04) |
| R-004 | `/privacy.html` contains third-party/DoubleClick cookie disclosure and **both** opt-out links: `https://adssettings.google.com` and `https://www.aboutads.info/choices` | building | `curl -A "<browser UA>" https://focusbro.net/privacy.html \| grep -c 'adssettings.google.com\|aboutads.info'` → ≥2. Source shipped 2026-07-05 (expanded route in `api/src/index.js`: third-party/DoubleClick disclosure, both opt-out links + youronlinechoices.eu, GDPR/CCPA rights, date-stamp). **Live curl pending** — this loop session's egress denies focusbro.net; verify after deploy |
| R-005 | Dismissible cookie-consent banner on the homepage (vanilla JS + localStorage, no external deps) | building | Curl homepage for the banner markup; manual dismiss-and-reload check. Source shipped 2026-07-05 (`#cookieConsent` banner in `public/index.html`, key `focusbro_cookie_consent_v1`, regenerated into `api/src/html.js`; no external deps). **Live curl pending** (egress blocked this session) |
| R-006 | Substantive About (what FocusBro is, the method, built by Latimer Woods Tech) and Contact (email + response expectation) pages — no marketing fluff, no testimonials | building | Curl both pages; human-judge substance. Source shipped 2026-07-05 (About = 4 paras incl. "The method" + "Who builds it"; Contact = Support/Privacy/Business sections with 2-business-day response expectation; no "AI", no testimonials). **Live curl pending** (egress blocked this session) |
| R-007 | 16 original guides (900–1300 words each) live at `/guides/<slug>.html`, server-rendered in the site shell with title + meta description + 2–3 internal links; `/guides/` index page; "Guides" nav link on homepage; all listed in `/sitemap.xml` with lastmod | live | `curl https://focusbro.net/guides/` → 200 listing 16; 2 random article URLs → 200 full text; sitemap lists them. **Content layer shipped + deployed 2026-07-05 (`2dd7351`): `api/src/guides/` module (array + `renderGuidePage`/`renderGuidesIndex` + generic route registration via `guides.forEach`), `/guides/` index, homepage footer "Guides" link, sitemap includes `/guides/` + guide URLs w/ lastmod. First 5 of 16 guides live (901–1040 words, named research: Ariga & Lleras, Kleitman, Balban et al./Cell Reports Medicine, R.&S. Kaplan/Berman, Ulrich — no fabricated citations).** Verified via deployed `focusbro-production` bundle (CF API): all 5 slugs + both renderers + Ariga/Kleitman/Cell Reports/Kaplan/Ulrich present. **Batch 2 shipped 2026-07-05 (`cc882ab`): 5 more guides → now 10/16 (887–1020 words each; named research: Steel 2007/Temporal Motivation Theory + Pychyl/Sirois; Newport + Leroy 2009 attention residue; Parkinson's Law 1955 + Rubinstein/Meyer/Evans 2001; caffeine adenosine + Drake et al. 2013; Barkley executive-function/self-regulation + body-doubling flagged thin-evidence — no fabricated citations). 53/53 vitest pass.** **Batch 3 (final) shipped 2026-07-05 (`94e6846`): last 6 guides → now 16/16 (928–1032 words each; named research: Van Dongen & Dinges 2003/Sleep + Xie/Nedergaard 2013/Science + Yoo/Walker 2007; Gollwitzer implementation intentions + Lally 2010 + Fogg anchoring + Milkman temptation bundling 2014; Gloria Mark interruption cost + Kushlev & Dunn 2015 + Ward et al. 2017 Brain Drain; Alan Hedge/Cornell ergonomics; Rauscher 1993 Mozart-effect debunk + Salamé & Baddeley + Perham serial recall + Mehta/Zhu/Cheema 2012; Zeigarnik 1927 + Masicampo & Baumeister 2011 + Allen GTD + Newport shutdown — no fabricated citations). 53/53 vitest pass.** Verified via deployed `focusbro-production` bundle (CF API, run 81 `28740291939` success): all **16** distinct slugs present, every new research marker present (proving deploy landed), non-regression clean (old pub-7015… 0, "Ad Space" 0, `XXXXXXXXXX` 0, consent banner intact, pomodoro intact, bare-word "AI" 0). **R-007 → live: content layer complete at 16/16.** (Egress-restricted session couldn't browser-curl; bundle evidence is the deployed origin code — founder browser spot-check of `/guides/` recommended) |
| R-008 | Timer app unbroken: homepage renders the Pomodoro timer/tasks/stats/sound UI | live | `curl -A "<browser UA>" https://focusbro.net/` → 200; Pomodoro/timer markup present (42 matches, verified 2026-07-04). Re-verify after every deploy |

## Non-functional

| ID | Requirement | Status | Verification |
|---|---|---|---|
| R-100 | Deploy pipeline is trustworthy: a failed `wrangler deploy` fails the workflow (pipefail or explicit exit-code capture), the CF API token authenticates, and a merge to main measurably updates worker `focusbro-production` | live | CF API `modified_on` for `focusbro-production` advances after merge + live curl shows new content. **Masking bug fixed 2026-07-05 (`18ee597`):** both deploy steps in `deploy.yml` `set -o pipefail` + capture `${PIPESTATUS[0]}`. **Confirmed 2026-07-05:** push of `2dd7351` → Deploy-to-Cloudflare run 28730429715 green, and the deployed `focusbro-production` bundle (CF API) now contains the brand-new Phase-3 guide strings that did not exist before the merge → the merge measurably updated the live worker. (Prior masked failure: run 28718694798 green while `modified_on` stayed 2026-05-06) |
| R-101 | `/sitemap.xml` and `/robots.txt` valid; sitemap includes every public page incl. all guides | building | Curl both; count URLs. Sitemap route updated 2026-07-05 (`2dd7351`) to append `/guides/` + every guide URL (with `<lastmod>`) via `guides.map`; deployed template confirmed in `focusbro-production` bundle. Now lists all **16** guide URLs (batch 3 `94e6846` completed the set); sitemap `guides.map` renders the full array. |
| R-102 | No secrets in source or wrangler vars: `JWT_SECRET` removed from `wrangler.toml` (root and `api/`) and set via `wrangler secret put` with a new random value; `node_modules/` + `.wrangler/` gitignored and untracked | roadmap | `grep JWT_SECRET wrangler.toml api/wrangler.toml` → no plaintext values; `git ls-files \| grep node_modules` → empty. Live 2026-07-04: **plaintext secret present in both files; node_modules committed** |
| R-103 | `/health` returns 200 on the branded domain | live | `curl -A "<browser UA>" https://focusbro.net/health` → 200 (verified 2026-07-04) |

## Accountability Core (Contender track — [issue #10](https://github.com/Latimer-Woods-Tech/focusbro/issues/10), Phase A)

> The reframe: FocusBro is a focus + accountability companion. These rows are engine-independent (push/text now; the voice call is Phase B, gated on `@latimer-woods-tech/voice-agent`). Shipped to source + tests 2026-07-05; `building` until a founder/live curl confirms the deployed API (this loop session's egress can't reach focusbro.net). Mechanic transplanted from wordis-bond (suite→scheduled-run→scored-outcome).

| ID | Requirement | Status | Verification |
|---|---|---|---|
| R-200 | Commitments model: `POST/GET /api/commitments` and `GET /api/commitments/:id` (auth-gated) persist a "word given" (title, start_at, checkin_at, channel, persona, timezone) in D1 `commitments`, and schedule a pending `commitment_checkins` row | building | `curl -H 'Authorization: Bearer …' -X POST https://focusbro.net/api/commitments -d '{"title":"start taxes","start_at":"…"}'` → 201 with commitment + warm prompt. Shipped (`api/src/accountability.js`, tables in `schema.sql` + `initializeDatabase`). Unit-tested (validation, defaults, voice-channel gate); **live curl pending** |
| R-201 | Check-in resolution: `POST /api/commitments/:id/checkin` with outcome `kept`/`missed`/`reschedule` updates the check-in + commitment status and transitions the kept-word streak in `accountability_streaks`; `GET /api/accountability/streak` returns it | building | `curl` the check-in endpoint per outcome; assert streak math. Shipped; `computeStreakAfter` unit-tested (kept +1/longest tracked, reschedule protected, miss resets current only). **Live curl pending** |
| R-202 | No-shame reschedule flow: a `reschedule` outcome PROTECTS the streak (never breaks the chain) and creates a follow-up commitment carrying the word forward; a `missed` outcome still offers to try again (never a dead end) | building | Assert reschedule leaves `current_streak` unchanged + spawns a new `active` commitment with `rescheduled_from` set; miss response has `offer_reschedule:true`. Unit-tested. **Live curl pending** |
| R-203 | The design LAW, enforced in code: no user-facing string shames, tallies misses, says "AI", or makes a clinical/treatment claim | building | `accountability.test.js` renders every copy path × both personas × edge streaks and fails the build on any shame word / `\bAI\b` / clinical term. Green: 18/18 accountability tests, 71/71 suite (2026-07-05). `accountability_streaks` has NO miss counter by design |
| R-204 | Configurable persona (`ally` calm / `hype` friend), both warm; the `voice` channel is politely gated until Phase B | building | Create with `persona:'hype'` → hype copy; `channel:'voice'` → 400 "coming soon". Unit-tested. **Live curl pending** |
| R-205 | Scheduled check-in delivery (a cron reads due `commitment_checkins` and sends the push/text nudge) | building | Worker cron (`[triggers] crontab` every minute) → `scheduled()` → `runDueCheckins()` scans `status='pending' AND scheduled_for<=now`, renders `checkinPromptCopy()` (anti-shame), delivers via real Web Push (RFC 8291/8292, `webpush.js`) or Telnyx text, then transitions `sent`/`skipped`/`failed` (attempts-capped, idempotent). Unconfigured channel → `skipped`, never crashes. New columns `delivered_at`/`attempts`/`last_error` + `users.phone`. Unit-tested incl. a real encrypt→decrypt crypto round-trip; **live curl pending** (egress-restricted session). Manual verify: `POST /api/internal/run-checkins` (guarded by `CRON_TRIGGER_KEY`) |
| R-206 | Server-side Web Push send path (`webpush.js`): aes128gcm payload encryption + VAPID ES256 JWT, config-guarded, gone-subscription (404/410) deactivation | building | Was missing — app only stored subscriptions. `sendWebPush(env, sub, payload)`; no-ops when VAPID keys absent. Round-trip + JWT-signature verified in `webpush.test.js`. **Live push to a device pending founder spot-check** |
| R-207 | Coach roster with a **consent gate**: `POST /api/coach/clients` (invite by email) creates a `pending` `coach_clients` link only; `GET /api/coach/invitations` + `POST /api/coach/invitations/:id/accept`\|`/decline` let the invited client opt in; a coach sees a client's kept-word momentum ONLY once `status='active'`. Enumeration-safe (unknown email → 202, no account disclosure); self-invite rejected | building | `api/src/coach.js` + `coach_clients` table (schema.sql + `initializeDatabase`, UNIQUE(coach,client)). 15/15 integration assertions (real router + in-memory D1): consent gate blocks client detail (404) and leaks no streak pre-accept; accept → active exposes momentum; non-invitee cannot accept. **Live curl pending** (egress-restricted session) |
| R-208 | Skeleton coach dashboard page `/coach/` (noindex, not in sitemap): read-only operator view — signs in via `/auth/login` (token in localStorage `focusbro_token`), renders roster with each active client's **kept-word streak + active-commitment count only, NO miss tally**; invite form; design-LAW copy | building | `router.get('/coach/')` self-contained page (vanilla JS, no external deps). Roster copy (`clientStatusLine`/`dashboardIntroCopy`/etc.) is momentum-framed; `coach.test.js` fails the build on any shame word (incl. `\bbehind\b`/`\bmiss\b`), `\bAI\b`, or clinical term across every roster path. Full white-label (cadence/script config, wholesale billing) = Phase C, gated on `@latimer-woods-tech/operator`. **Live curl pending** (founder browser spot-check of `/coach/` recommended) |
| R-210 | Consent by construction (TCPA): a text/voice check-in cannot be delivered unless a `granted` `contact_consent` row exists for that channel; the delivery cron consumes `evaluateContactGate()` and marks a check-in `skipped:no_consent` / `skipped:opted_out` otherwise. Express consent is captured with the exact disclosure stored verbatim (`consent_text` + `consent_version`); push is app-UX and never gated | building | `api/src/consent.js` (`evaluateContactGate`, `consentLanguage`, `CONSENT_VERSION`) + `contact_consent` table (schema.sql + `initializeDatabase`, `UNIQUE(user_id,channel)`). Cron gate wired in `checkins-cron.js`; `POST/GET /api/consent`. `consent.test.js` + `checkins-cron.test.js` assert no-consent/opted-out → skip, granted → send. **Live curl pending** (egress-restricted session) |
| R-211 | Quiet hours enforced in the recipient's local time: a due text/voice check-in inside the person's quiet window is HELD (`deferred`, no attempt bump) and delivered once the window passes — never dropped, never sent mid-night | building | `isWithinQuietHours()` (IANA-tz aware, handles overnight wrap) + the cron `defer` path (summary `deferred++`, row left `pending`). `consent.test.js` pins same-day/overnight/timezone cases; `checkins-cron.test.js` pins defer-then-deliver. **Live curl pending** |
| R-212 | One-word opt-out honored instantly + durably: STOP (CTIA family) via the app (`POST /api/consent/opt-out`) or inbound SMS webhook (`POST /api/webhooks/telnyx/inbound`, Ed25519-verified when `TELNYX_PUBLIC_KEY` set) sets `status='revoked'`; START re-grants. The design LAW holds — leaving is met with warmth, never a guilt trip | building | `isStopKeyword/isStartKeyword/isHelpKeyword`, `verifyTelnyxSignature`, revoke path. `consent.test.js` covers keyword detection + the design-LAW scan over `consentCopySurface()` (opt-out/HELP/panel copy: no shame word, no `\bAI\b`, no clinical term). **Live curl pending**. NOTE: org-wide durable consent ledger is the shared CRM/consent schema (Foundry #2001) — synced in the Phase B integration, not hand-rolled here |
| R-209 | Consumer accountability front door `/me/` (noindex, no-store, not in sitemap): the person-facing door to the accountability API — sign in **or create an account**, give-your-word form (title, when, persona, channel), kept-word streak, and resolve a check-in as did-it / not-yet / move-it. **A `missed` word renders as an open door ("Ready when you are") with one-tap try-again, never a failure tally**; no miss counter anywhere on the page. Homepage footer links to it | building | `api/src/me.js` (`renderMePage()` + copy engine) + `router.get('/me/')`; reuses `/api/commitments`, `/api/commitments/:id/checkin`, `/api/accountability/streak`, `/auth/login`, `/auth/register` (no new endpoints). Self-contained vanilla JS, no external deps; datetime-local → ISO(UTC) client-side. `me.test.js` fails the build on any shame word (incl. `\bmiss(ed)?\b`/`\bbehind\b`/`\bfail\b`), `\bAI\b`, or clinical term across the curated consumer copy surface + `statusPresentation` for every status. **Live curl pending** (egress-restricted session; founder browser spot-check of `/me/` recommended) |
| R-213 | Recurring check-in cadence — "the bro who calls you every day at the same time." A commitment carries a `recurrence` (`none`\|`daily`\|`weekdays`) + `local_time` anchor; each occurrence lands at the same recipient-local wall-clock time (DST-correct). A recurring commitment stays `active` (never resolves terminally); its next occurrence is materialized both on in-app resolve and by the delivery cron once the current check-in leaves `pending` — so the rhythm never stalls even if a check-in is never answered or no channel is configured yet. Mechanic reused from wordis-bond's scheduled-run cadence (cadence on the parent + materialized child rows) | building | `nextOccurrenceISO()`/`pickRecurrence()`/`parseLocalTime()`/`localTimeFromISO()` in `accountability.js` (pure, Intl-based, no Node built-ins); `recurrence`+`local_time` columns (schema.sql + `initializeDatabase` CREATE + idempotent ALTER); `materializeNextOccurrence()` in `checkins-cron.js` (idempotent, no-op for one-shots / non-active) + `ensureNextOccurrence()` on resolve; `/me/` "Repeat" selector sends `recurrence`+`local_time`. Unit-tested: DST spring-forward holds 08:40 local, weekdays skips Sat/Sun, materialize-on-skip + idempotency (154/154 suite). **Live curl pending** (egress-restricted session) |
| R-214 | Founder dogfood seed: `POST /api/internal/seed-dogfood` (guarded by `CRON_TRIGGER_KEY`, 404s when unset) registers the founder's standing daily commitment — "send one outreach item" at 08:40 America/New_York (Factory#1960) — as a recurring commitment so the bro starts checking in the moment a channel is configured. Idempotent (re-run returns the existing commitment); requires the account to exist (`/me/` signup first) | building | `router.post('/api/internal/seed-dogfood')` in `index.js`; looks up user by email (default `adrper79@gmail.com`), creates a `daily` recurring commitment + first pending check-in via `nextOccurrenceISO()`. Channel defaults to `text` (gated by TCPA consent R-210 + Telnyx) — until a channel + consent are set the cron cleanly `skipped`s and re-materializes. **Founder action to activate: sign up at `/me/`, grant text consent, then `curl -X POST /api/internal/seed-dogfood -H 'x-cron-key: …'`** |
| R-215 | Two-way text check-ins: an inbound SMS reply resolves the person's open text check-in. `detectCheckinReply()` reads "done/did it/yep" → `kept` and "later/not yet/tomorrow" (incl. negated "not done"/"haven't") → the no-shame `reschedule`; anything unreadable → a warm clarifying nudge that leaves the check-in OPEN (we never assume a miss from a message we couldn't read). Resolution flows through the shared `applyCheckinOutcome()` core (same streak math + recurring re-queue as the in-app path), and a warm confirmation SMS is sent back — so a text check-in is a real two-way conversation, the text-channel stand-in for the Phase B voice moat. STOP/HELP still win first; a bare "yes" is only treated as opt-in re-subscribe when the person is actually opted out, otherwise it answers the check-in | building | `detectCheckinReply`/`applyCheckinOutcome`/`smsKeptReplyCopy`/`smsRescheduleReplyCopy`/`smsAmbiguousReplyCopy` in `accountability.js`; inbound path extended in `consent.js` `/api/webhooks/telnyx/inbound` + guarded fail-safe `sendSms()`. No schema change (reuses `commitment_checkins.status='sent'` + `responded_at`). `two-way-checkins.test.js`: reply detection incl. negation + bare answers, design-LAW scan over every reply string, `applyCheckinOutcome` streak/reschedule/recurring-requeue, and a real-`itty-router` webhook harness (done→kept+confirmation, later→reschedule, bare-yes→answer-not-resubscribe, STOP wins, unclear→open+ask, no-open→silent). 174/174 suite (+20). **Live curl pending** (egress-restricted session; a founder inbound-SMS spot-check once Telnyx is live closes it) |
| R-216 | Coach-side read-only cadence view: the coach detail endpoint `GET /api/coach/clients/:clientId` surfaces each active commitment's self-set rhythm — `recurrence`/`local_time`/`timezone` plus a human `cadence` label ("Every day at 08:40", "Weekdays", "One-time") — and the `/coach/` page adds a per-client "View rhythm" toggle that lists a client's commitments with their cadence. Momentum-only: cadence describes when a person asked the bro to show up, **never a miss tally**; consent gate unchanged (only `active` clients, 404 otherwise). Foundation for the Phase C white-label rhythm/script config (still gated on `@latimer-woods-tech/operator`) | building | Shared pure `describeCadence({recurrence,localTime})` in `accountability.js` (reused by `/me/` + coach, deterministic); `coach.js` detail maps `cadence` + `rhythm_intro`/`rhythm_empty` copy; `/coach/` page `renderRhythm()` + delegated `.rhythm-toggle` fetch. `coach.test.js`: `describeCadence` unit cases (one-shot/daily/weekdays, ±local_time, garbage→total) + the new rhythm copy folded into the design-LAW scan (no shame word / `\bAI\b` / clinical term). 178/178 suite (+4). **Live curl pending** (egress-restricted session; founder browser spot-check of a client's "View rhythm" on `/coach/` closes it) |
| R-217 | Set-it-down (the blameless exit): `POST /api/commitments/:id/release` moves an active commitment to a terminal `released` state, cancels its still-waiting check-ins (`pending`/`deferred` → `cancelled`) so the delivery cron stops ringing, and **never reads or writes the kept-word streak** — releasing protects the chain by construction. Plans change; before this the only exits from an active word were kept/missed/reschedule, so a word a person no longer intends to keep sat `active` and got nudged forever. `/me/` renders a quiet "Set it down" action on active commitments (with a warm confirm) and a blameless `Set down` status pill. Design LAW: the confirmation is glad you told us, never disappointed — not a miss, not a scold | building | `releaseConfirmCopy({persona})` + `/api/commitments/:id/release` route in `accountability.js` (idempotent; 404 non-owned, 401 no-auth; the cron's existing `commitment_status !== 'active'` guard means a released recurring word is never re-materialized); `me.js` adds `released` to `COMMITMENT_STATUSES`, `statusPresentation('released')`, `releaseActionLabel()`, the client STATUS map + delegated `data-act="release"` handler. Tests: `release.test.js` drives the real route through itty-router + an in-memory D1 double — asserts status=`released`, pending/deferred check-ins cancelled, **no `accountability_streaks` access at all**, 404/401 paths — plus the design-LAW scan (no shame incl. `quit`/`gave up`, no `\bAI\b`, no clinical term) and a "streak is safe" reassurance check; `releaseConfirmCopy` folded into `accountability.test.js` copy-law samples; `me.test.js` lifecycle assertion updated to five states. 187/187 suite (+9). **Live curl pending** (egress-restricted session; founder browser spot-check of "Set it down" on `/me/` closes it) |
| R-219 | Pause / resume a recurring rhythm (take a break, never a goodbye): `POST /api/commitments/:id/pause` suspends a repeating word — moves it to a terminalless `paused` state, cancels its still-waiting check-ins (`pending`/`deferred` → `cancelled`) so the delivery cron stops ringing, and (because the cron's materializer only re-queues an `active` commitment) schedules nothing while paused. `POST /api/commitments/:id/resume` returns it to `active` and materializes the next occurrence from now (no backlog of the days away). Both paths **never read or write the kept-word streak** — a pause is not a miss, by construction. Pause is for a *rhythm*: a one-shot word is warmly refused (409, "set it down or move it"), as is a non-active pause / non-paused resume (409, no mutation). `/me/` shows a **Pause** action on active recurring words and a **Resume** action on paused ones, with a `Paused` status pill (calm tone, never red). The gap it closes: before this, the only ways off an active recurring word were to resolve each occurrence, set it down (release — terminal, loses the whole setup), or absorb nudges you can't answer while away — a guilt pile the design LAW forbids | building | `pauseConfirmCopy`/`resumeConfirmCopy` + `/api/commitments/:id/pause`\|`/resume` routes in `accountability.js` (idempotent-safe: 404 non-owned, 401 no-auth, 409 non-recurring/non-active/non-paused; resume reuses `ensureNextOccurrence`, idempotent); `me.js` adds `paused` to `COMMITMENT_STATUSES`, `statusPresentation('paused')` (tone `moved`, label "Paused"), `pauseActionLabel()`/`resumeActionLabel()`, the client STATUS map + `data-act="pause"`/`"resume"` handlers (Pause only on recurring active). No schema change (free-form `status` TEXT; comment updated to list `released`/`paused`). Tests: `pause-resume.test.js` drives both routes through itty-router + an in-memory D1 double — asserts pause→`paused`+check-ins cancelled, resume→`active`+next occurrence materialized (future) + no double-schedule, **no `accountability_streaks` access on either path**, 409 one-shot/non-active/non-paused (no mutation), 404/401 — plus the design-LAW scan (no shame incl. `miss`/`behind`/`catch up`, no `\bAI\b`, no clinical term; resume names the next check-in); pause/resume copy folded into `accountability.test.js` copy-law samples; `me.test.js` lifecycle assertion updated to six states. 216/216 suite (+16). **Live curl pending** (egress-restricted session; founder browser spot-check of "Pause"/"Resume" on `/me/` closes it) |
| R-218 | "I'm on it" snooze (keep the bro present): `POST /api/commitments/:id/snooze` re-arms the latest still-open check-in (`pending`/`sent`/`deferred` → `pending`, `scheduled_for` pushed a few minutes out, `attempts` reset, `responded_at` cleared), or opens a fresh `pending` one if none is open — so the nudge comes back without moving the word or resetting the rhythm. The requested interval is clamped to a sane window (`SNOOZE_MIN_MIN`=5 / default 15 / `SNOOZE_MAX_MIN`=180) so a snooze can never quietly become a reschedule. A real accountability friend's third answer, between "I did it" and "move the whole thing"; it beats the swipe-away failure mode a push nudge invites. The kept-word streak is **never read or written** (a snooze is not a resolution, by construction) and the commitment row is never moved; a non-`active` word is warmly refused (409, no mutation). `/me/` adds an "I'm on it" action on active commitments | building | `clampSnoozeMinutes()` + `snoozeConfirmCopy({persona,minutes})` + `/api/commitments/:id/snooze` route in `accountability.js` (idempotent-safe: 404 non-owned, 401 no-auth, 409 non-active; no schema change — reuses `commitment_checkins.attempts`/`last_error`/`responded_at`); `me.js` adds `snoozeActionLabel()` to the copy surface, an `I'm on it` button, a `snooze()` client fn + delegated `data-act="snooze"` handler. Tests: `snooze.test.js` drives the real route through itty-router + an in-memory D1 double — asserts re-arm-to-future/pending/attempts-reset, fresh-insert when none open, minutes clamp, **no `accountability_streaks` access** and **no `UPDATE commitments`**, 409 non-active (no mutation), 404/401 paths — plus `clampSnoozeMinutes` unit cases and the design-LAW scan (no shame incl. `don't forget`/`hurry`, no `\bAI\b`, no clinical term; copy names the interval); `me.test.js` copy-surface scan covers the new label. 200/200 suite (+13). **Live curl pending** (egress-restricted session; founder browser spot-check of "I'm on it" on `/me/` closes it) |
| R-220 | Kept-word log (a positive record, never a wall of misses): `GET /api/accountability/kept` returns the list of every word this person KEPT — most recent first, each joined to its title — plus the lifetime `total_kept` (from the kept-word streak row, honest past the rendered 50-row window) and a warm header line (`keptLogCopy`). Momentum-only **by construction and by the design LAW**: the query reads ONLY `status='kept'` check-ins, so a set-down or moved word can never appear, and there is deliberately no "missed" list anywhere in the product. Where a to-do app becomes a guilt engine is the history view; this is the opposite — the record of you showing up. `/me/` renders a "Words you kept" card (green ✓ per row, warm empty-state invitation) that refreshes on sign-in and whenever a word is kept | building | `keptLogCopy({total,persona})` + `GET /api/accountability/kept` route in `accountability.js` (auth-gated 401; empty-safe → `[]`/0/invitation; lifetime total from `loadStreak().total_kept`); `me.js` adds `keptLogHeadingCopy()`/`keptLogEmptyCopy()` to the copy surface, the card markup + `.keptrow` styles, and a `loadKept()` client fn wired into `enterApp()` + the kept-resolution refresh. Additive index `idx_checkins_user_status` on `commitment_checkins(user_id, status)` in `schema.sql` (idempotent `IF NOT EXISTS`). Tests: `kept-log.test.js` drives the real route through itty-router + an in-memory D1 double — asserts the kept list + lifetime total + message, the momentum-only query shape (`status = 'kept'`, **no `missed` in the query**), empty-safe path, and 401-without-query — plus the design-LAW scan (no shame incl. `miss`/`behind`, no `\bAI\b`, no clinical term; empty state points forward to the "first" kept word; grammatical singular/plural); `me.test.js` render + copy-surface assertions cover the card, the heading/empty copy, and the `/api/accountability/kept` wiring. 225/225 suite (+9). **Live curl pending** (egress-restricted session; founder browser spot-check of the "Words you kept" card on `/me/` closes it) |
| R-221 | Edit a commitment in place (a small change never costs the streak): `POST /api/commitments/:id/edit` merges a subset of fields — title, details, persona, channel, and the whole schedule (recurrence/local_time/timezone/start_at) — over an existing word and persists it, so a reworded title or a nudged time keeps the SAME commitment instead of forcing set-it-down-and-recreate. Only an OPEN word can be edited (active or paused); a wrapped-up word (kept/moved/set down) is warmly refused (409, "give a fresh one"). The kept-word streak is **never read or written** — an edit is not a resolution. When the schedule moves, the outstanding check-in is cancelled and, for a still-active word, a fresh one is queued at the new time; a paused rhythm is left quiet (resume schedules it from now). Turning a one-shot into a rhythm keeps the same time-of-day when none is given. `/me/` gets an **Edit** action on active + paused cards that reveals an inline title/when/repeat form (Save/Cancel) | building | Pure `buildCommitmentEdit(existing, body, nowISO)` (merge + normalize + `scheduleChanged` flag, no DB, no streak) + `editConfirmCopy({persona,scheduleChanged,when})` + `/api/commitments/:id/edit` route in `accountability.js` (idempotent-safe: 404 non-owned, 401 no-auth, 409 non-open, 400 empty-title/no-op; status preserved — edit is not resume; schedule move → cancel `pending`/`deferred` + insert fresh only while active). No schema change (reuses existing `commitments` columns). `me.js` adds `editActionLabel()` to the copy surface, the **Edit** button on active + paused cards, an inline `editFormHTML(c)` (title/when/repeat, pre-filled) + `openEdit`/`saveEdit`/`closeEdit` client fns + delegated `data-act="edit"`/`edit-save`/`edit-cancel` handlers, and `.editform` styles. Tests: `edit.test.js` drives the real route through itty-router + an in-memory D1 double — asserts title-only edit updates + re-queues nothing, time edit cancels+inserts, paused edit cancels-but-stays-quiet, **no `accountability_streaks` access**, 409 wrapped-up (no mutation), 400 empty/no-op, 404/401 — plus `buildCommitmentEdit` unit cases (carry-over, recompute, one-shot↔rhythm, voice/unknown-channel refusal) and the design-LAW scan (no shame incl. `miss`/`behind`/`catch up`, no `\bAI\b`, no clinical term; names next check-in only when schedule moved; reassures streak intact); `me.test.js` renderMePage assertion covers the Edit button + inline form + `/edit` wiring. 246/246 suite (+21). **Live curl pending** (egress-restricted session; founder browser spot-check of "Edit" on `/me/` closes it) |
| R-222 | Per-word detail view (look at one word's momentum): `GET /api/commitments/:id/detail` returns a single commitment plus its cadence (`describeCadence`), the next outstanding check-in (`pending`/`sent`/`deferred`, soonest first, **active words only**), and the timeline of check-ins **KEPT on THIS word** — reading `status='kept'` ONLY, so a set-down or missed check-in can never appear; `kept_count` comes from a `COUNT(*)` so it stays honest past the rendered 50-row window. The gap it closes: the forward loop + the cross-word kept log existed, but there was no way to look back at ONE word's rhythm and wins, and the only per-commitment endpoint (`GET /api/commitments/:id`) returned ALL check-ins incl. misses with no design-LAW-safe front end. `/me/` gains a **View** action on every card (active/paused/missed and wrapped words) that toggles an inline panel showing the cadence, the next check-in, a warm momentum line, and the kept ✓ timeline — never a miss list | building | `commitmentDetailCopy({persona,keptCount})` + `/api/commitments/:id/detail` route in `accountability.js` (404 non-owned, 401 no-auth; kept timeline + `COUNT(*)` both filter `status='kept'`; upcoming-check-in query runs only when `status='active'`; no schema change — existing `idx_checkins_commitment` covers it). `me.js` adds `detailActionLabel()`/`detailKeptHeadingCopy()`/`detailNextLabelCopy()` to the copy surface, a **View** button on every card, an inline `.detail` panel container + `openDetail`/`renderDetail` client fns (toggle + momentum-only render) + delegated `data-act="view"` handler, and `.detail` styles. Tests: `detail.test.js` drives the real route through itty-router + an in-memory D1 double — asserts the word+cadence+next+kept payload, the timeline reads `status='kept'` only with **no query selecting a miss**, `kept_count` from `COUNT(*)` (honest past the window), next-check-in suppressed AND its query skipped for a non-active word, empty-safe, 404/401 — plus the design-LAW scan on the detail copy (no shame incl. `miss`/`behind`, no `\bAI\b`, no clinical term; empty state points forward); `me.test.js` renderMePage assertion covers the View button + `data-detail` panel + `/detail` wiring. 259/259 suite (+13). **Live curl pending** (egress-restricted session; founder browser spot-check of "View" on `/me/` closes it) |

| R-223 | Conversational reschedule over text ("no problem — when do you want to try again?"): a "later" SMS reply no longer punts the person into the app — the bro **asks for a time right there over text and accepts the answer**. The design LAW's literal promise, finally on the wire. On "later"/"not yet"/etc., the check-in moves to a new `awaiting_time` state and the bro replies "when do you want to try again? (text 3pm / in 1 hour / tomorrow 9am)"; the person's next reply is parsed by `parseWhenReply()` (DST-correct, recipient-local: relative "in N min/hours", clock "3pm"/"14:00"/"noon", bare hour → soonest future, "tonight", "tomorrow [time]" defaulting to their usual check-in time) and **this same check-in is re-pended at that instant** (`status='pending'`, `scheduled_for` moved, attempts reset) — the streak is NEVER read or written (a reschedule protects the chain by construction), and the next recurring occurrence was already materialized at delivery so the rhythm never stalls. A late "done" while awaiting is still honored as kept; an unreadable time re-asks warmly (never assumes a miss). The whole conversation stays on the channel that reached them — the point of the moat. Also: the outbound **text** nudge now carries a one-line reply hint (`checkinReplyHint`, text-only — push keeps its in-app buttons) so the two-way loop is discoverable. `release`/`pause`/`edit` cancel and `snooze` re-arms `awaiting_time` rows alongside `pending`/`deferred` | building | `parseWhenReply()` + `formatWhenLocal()` (pure, Intl-based, reuse `zonedWallToUtcMs`/`tzParts`; no Node built-ins) + `smsAskWhenCopy`/`smsRescheduledCopy`/`smsWhenUnclearCopy`/`checkinReplyHint` in `accountability.js`; inbound webhook in `consent.js` restructured to a single newest-open query over `status IN ('sent','awaiting_time')` + the awaiting-time state machine (kept / parse-time→re-pend / re-ask); `deliverCheckin` appends the reply hint for `channel='text'` only. No schema change (free-form `status` TEXT gains the `awaiting_time` value; the three cancel queries + snooze re-arm updated to include it). Tests: `two-way-checkins.test.js` — `parseWhenReply` relative/clock/tomorrow/DST/vague-null/horizon cases, `formatWhenLocal` same-day/tomorrow, the webhook flow (later→ask-when+`awaiting_time`+no resolution+no "in the app"; time-while-awaiting→re-pend+streak-safe; late-done→kept; unreadable→re-ask), and the design-LAW scan over the new copy (no shame/`\bAI\b`/clinical); `checkins-cron.test.js` — the text nudge carries "reply DONE / LATER / try again" and no scold. 277/277 suite (+18). **Live curl pending** (egress-restricted session; a founder inbound-SMS spot-check once Telnyx is live — text "later", then a time — closes it) |
| R-224 | Coach-visible "next check-in" (the concrete moment the bro next shows up): the coach detail endpoint `GET /api/coach/clients/:clientId` now attaches, per active commitment, the soonest OUTSTANDING check-in (`next_checkin` ISO) plus a warm recipient-local label (`next_checkin_label`, e.g. "Next up tomorrow at 8:40 AM"); the `/coach/` "View rhythm" panel renders that line under each commitment's cadence. The whole product thesis is a companion who shows up ON A SCHEDULE, so a coach seeing only the abstract cadence ("Every day at 08:40") was missing the concrete next occurrence. Momentum-only **by construction and by the design LAW**: `next_checkin` reads only a future/outstanding check-in (`status IN ('pending','sent','deferred')`, `MIN(scheduled_for)`) — a moment about to be KEPT — so it can never surface a miss or an "overdue"; when nothing is scheduled yet the copy stays forward-looking ("Next check-in lining up."). Consent gate unchanged (only `active` clients, 404 otherwise). Foundation for the Phase C white-label (still gated on `@latimer-woods-tech/operator`) | building | Reuses pure `formatWhenLocal(iso, timezone, nowISO)` from `accountability.js` (Intl-based, DST-correct, no Node built-ins) via a new `nextCheckinCopy({iso,timezone,nowISO})` helper in `coach.js`; the detail route adds ONE grouped query (`GROUP BY commitment_id`, not N per row) mapping each active commitment to its soonest outstanding check-in, then attaches `next_checkin` + `next_checkin_label`. `/coach/` `renderRhythm()` appends a `.rhythm-next` line when present. No schema change (reads existing `commitment_checkins`). Tests: `coach.test.js` — `nextCheckinCopy` same-day/tomorrow/none/garbage cases + the new copy folded into the design-LAW scan (no shame word / `\bAI\b` / clinical term). 295/295 suite (+4). **Live curl pending** (egress-restricted session; founder browser spot-check of "View rhythm" on `/coach/` — the next-up line under each cadence — closes it) |
| R-225 | In-app "Move it" shares ONE time parser with the SMS channel (the warm forms work everywhere): the in-app reschedule used to demand a rigid `2026-07-07 14:00` while a text reply already understood "in 30 min", "tomorrow 9am", "3pm", "noon" — two parsers, and the app was the colder, stricter one, exactly the friction the anti-shame design LAW exists to kill. Now `/api/commitments/:id/checkin` (reschedule) accepts a natural-language `when_text` parsed **server-side by the same `parseWhenReply` the SMS reschedule uses** (DST-correct, recipient-local, never-past, ≤14-day horizon), so the in-app "Move it" understands the person's own words identically to a text reply. An explicit ISO `new_start_at` still works (backward compatible); an unreadable time is refused warmly with the shared `smsWhenUnclearCopy` voice (400) and **writes nothing** — never assume a time, and (per the LAW) never a miss. The follow-up word now also carries the person's timezone instead of a silent UTC | building | Reschedule branch of `/api/commitments/:id/checkin` in `accountability.js` resolves the new start from `new_start_at` (ISO) OR `when_text` via `parseWhenReply({nowISO,timezone: commitment.timezone, defaultTime: commitment.local_time})`, then feeds `validateCommitmentInput` (now passed `timezone: commitment.timezone`); unreadable `when_text` → `smsWhenUnclearCopy` 400, no INSERT. `me.js` "Move it" prompt reworded to warm examples and sends `{ when_text }` (drops the client-side `toISO` pre-parse + rigid-format alert; the datetime-local picker on the give-a-word form is unchanged). No schema change. Tests: `reschedule-when-text.test.js` drives the real route through itty-router + an in-memory D1 double — asserts `when_text:'in 30 min'` creates a follow-up ~30 min out carrying the person's zone + `rescheduled_from`, the shared warm forms (`tomorrow 9am`/`3pm`/`in 2 hours`/`tonight`) all parse, an unreadable time → 400 shared copy + **no `INSERT INTO commitments`**, and `new_start_at` ISO still works. 299/299 suite (+4). **Live curl pending** (egress-restricted session; a founder browser spot-check of "Move it" on `/me/` — typing "in 30 min" — closes it) |
| R-226 | The give-a-word form accepts natural-language time — the SAME `parseWhenReply` now runs on EVERY surface (the very first word too, not just the reschedule + SMS): the reschedule (R-225) and the text channel already understood "in 30 min", "tomorrow 9am", "3pm", but the initial "When?" still demanded a `datetime-local` picker — the last surface making a person fight a time format just to give their word, exactly the friction the anti-shame LAW exists to kill. Now `POST /api/commitments` resolves a natural-language `when_text` server-side via the shared `parseWhenReply` (DST-correct, recipient-local, never-past, ≤14-day horizon); for a repeating word the same-time-each-day anchor (`local_time`) is derived from the resolved instant, so no separate field is needed. An explicit ISO `start_at` still works (backward compatible with the API and any client that sends an instant); an unreadable time is refused warmly with the shared `smsWhenUnclearCopy` voice (400) and **writes nothing** — never assume a time, and (per the LAW) never a miss. One parser, one voice, on every door a person gives their word through | building | Create handler in `accountability.js` resolves `when_text`→`start_at` via `parseWhenReply({nowISO,timezone: body.timezone, defaultTime: body.local_time})` **only when no explicit `start_at` is supplied** (backward compatible), then feeds the existing `validateCommitmentInput` (which already derives `local_time` from `start_at` for a recurring word via `localTimeFromISO`); unreadable `when_text` → `smsWhenUnclearCopy` 400, no INSERT. `me.js` give-a-word "When?" input becomes a warm natural-language text field (`placeholder="in 30 min, tomorrow 9am, 3pm"`) sending `{ when_text }` and dropping the `datetime-local` picker + client `toISO`/`local_time` slicing (the edit-in-place picker is unchanged). No schema change. Tests: `create-when-text.test.js` drives the real `POST /api/commitments` through itty-router + an in-memory D1 double — asserts `when_text:'in 30 min'` starts the word ~30 min out carrying the person's zone, the shared warm forms (`tomorrow 9am`/`3pm`/`in 2 hours`/`tonight`) parse, a recurring `tomorrow 9am` derives `local_time='09:00'`, an unreadable time → 400 shared copy + **no `INSERT INTO commitments`**, and an explicit ISO `start_at` still works. 304/304 suite (+5). **Live curl pending** (egress-restricted session; a founder browser spot-check on `/me/` — type "in 30 min" into "When?" and give a word — closes it) |
| R-227 | Gentle first-run welcome on `/me/` (seed the first word — the activation moment, never a scold about an empty page): a person who signs in with zero words given used to land on a bare "No words given yet" line — the exact blank-page friction that stalls the ADHD/executive-function audience before the product ever proves itself. Now, when the commitments list is empty, `/me/` reveals a warm first-run panel above the give-a-word form (`firstRunHeadingCopy` "Let's give your first word." + `firstRunBodyCopy` "…big or small…you can change it any time." + a row of low-stakes example seeds) and gently places the cursor in the title field (focused **once** so a later reload never steals focus). Tapping an example seed (`reply to that one email` · `a ten-minute tidy` · `go for a short walk` · `drink a glass of water`) **fills the title only** and moves focus to "When?" — the person still sets the time and gives the word themselves, so a time is **never assumed** and nothing auto-commits (per the LAW, never a miss). The panel disappears the instant a first word exists. Momentum-only and blameless by construction: it is an invitation, not a record, so there is no miss surface to leak | building | `me.js` adds `firstRunHeadingCopy()`/`firstRunBodyCopy()`/`firstRunExamplesLabel()`/`firstRunExamples()` (all folded into `meCopySurface()` so the design-LAW gate scans them), a `#firstRun` card (server-rendered seed chips, HTML-escaped) above the streak card, a `updateFirstRun(commitments)` client fn wired into `loadList()` (empty→show+focus-once, any word→hide), a delegated `#seedRow` click handler that sets `#title` and focuses `#startAt` (no fetch/submit), and `.firstrun`/`.seed`/`.seedrow` styles. No API/schema change (reads the existing `/api/commitments` load). Tests: `me.test.js` adds a first-run block — non-empty warm copy + ≥3 seeds, the panel/copy/`data-seed` chips render, the `class="card firstrun hidden"` default + `updateFirstRun` toggle, a seed tap fills the title only (no submit path), and the copy folded into the design-LAW surface (auto-scanned for shame incl. `miss`/`behind`, `\bAI\b`, clinical). 309/309 suite (+5). **Browser-driven verification done this session** (headless Chromium against a local origin: empty list → panel visible + title auto-focused; seed tap → title `"reply to that one email"` + focus on `startAt`; non-empty list → panel hidden). **Live curl pending** (egress-restricted session; a founder browser spot-check on `/me/` with a fresh account closes it) |
| R-229 | Your own kept-word **momentum sparkline** on `/me/` (their wins, their eyes): the person's own `GET /api/accountability/kept` now attaches a `momentum` block — a per-day count of words they KEPT over a trailing 14-day window in their own timezone — and `/me/` renders it as a small bar sparkline (own card, "Your momentum") above the kept-word log, with a first-person summary ("You kept N words over the last 14 days. Your best day: M kept."). Same shape the coach sees (R-228), turned around for the person themselves: first-person voice, and the card stays **hidden until a first word is kept**, so a brand-new page is never a chart of nothing. Momentum-only **by construction and by the design LAW**: built from `status='kept'` check-ins ONLY, so a quiet day is a short/grey bar — the *absence* of a win, never a miss — and an all-quiet window is a flat baseline reading as a clean page ("every window is a fresh start"). Day boundaries DST-correct (Intl local-calendar bucketing). The momentum engine was extracted to a shared `momentum.js` so the coach and consumer views share ONE tested shape without sharing a voice | building | New pure module `api/src/momentum.js` — `MOMENTUM_WINDOW_DAYS`, `localDayInZone`, `bucketKeptByDay`, `sparklineBars`, and a voice-injectable `buildMomentum({...,intro,summary})`; the math moved out of `coach.js`, which now imports + re-exports it and keeps its third-person `buildMomentum` wrapper (coach API unchanged). First-person copy `momentumSelfHeadingCopy()`/`momentumSelfIntroCopy()`/`momentumSelfSummaryCopy({total,days,peak})` in `accountability.js` (the API that emits it; folded into `meCopySurface()` so the design-LAW gate scans it). The kept route adds a representative-timezone lookup (most-recent commitment zone, UTC fallback) + one windowed `status='kept'` query (`responded_at >= cutoff`, LIMIT 1000) → `buildMomentum`, attached as `momentum`. `me.js` adds a `#momentumCard` (hidden by default) + `renderMomentum(m)` client fn (scaled `.spark-bar` bars, quiet day = `.spark-bar.zero` grey, `title="date: N kept"`, `role="img"` aria-label) wired into `loadKept()`, + `.spark`/`.momentum-summary` styles. No schema change (reads existing `commitment_checkins`; import cycle avoided — `accountability.js` imports only the pure `momentum.js`). Tests: new `momentum.test.js` (+6) pins the injected-voice contract (caller intro/summary verbatim, empty-voice default, kept-only summary args) + kept-only baseline behavior; `me.test.js` (+5+card assertions) — first-person voice (`you`/not `their`), singular/plural, quiet clean-page, LAW scan, and the `#momentumCard`/`renderMomentum`/`data.momentum`/`.spark-bar`/`role="img"` render wiring; `kept-log.test.js` (+3) drives the real route through itty-router + a D1 double returning windowed timestamps — asserts the `momentum` block shape + first-person voice, the windowed query reads `status='kept'` with **no `missed`**, and a no-kept person gets a quiet clean-page block. Coach parity re-verified (`coach.test.js` green off the re-exports). 339/339 suite (+14); `eslint src` 0 errors; Node-driven end-to-end render this session (NY-tz week → correct scaled sparkline `▁…▅▁▅▁▅█`, peak day full bar, first-person summary, LAW-clean; quiet → hidden). **Live curl pending** (egress-restricted session; a founder browser spot-check of the "Your momentum" sparkline on `/me/` with a few kept words closes it) |
| R-230 | Gentle re-entry on `/me/` (a returning person is welcomed back, never cold-started): a person who HAS given words before but has nothing in flight right now — every word kept, moved, or set down — used to land on their history list with no warm door back in, and a returning person is not a first-timer, so the first-run pitch ("Let's give your first word.") correctly stays hidden and left them nothing. Now `/me/` shows a distinct "Welcome back." re-entry panel above the give-a-word form (`reentryHeadingCopy` + `reentryBodyCopy` "Good to see you again. No catching up and no gap to explain — just pick the next thing…"). A single `entryState(commitments)` decides which door shows so the two banners can NEVER both appear: `'first-word'` (zero words → the first-run invitation), `'welcome-back'` (has words, none active/paused → the re-entry door), `'in-flight'` (an active or paused word exists → no banner, just the work). Anti-shame **by construction**: the re-entry copy names no gap and no lapsed streak — it points only forward, per the design LAW's "no problem — when do you want to try again?" promise applied to the re-engagement moment | building | `me.js` adds `reentryHeadingCopy()`/`reentryBodyCopy()` (both folded into `meCopySurface()` so the design-LAW gate scans them) + a pure exported `entryState(commitments)` (single source of truth: `first-word`/`welcome-back`/`in-flight`), a `#reentry` card (reuses `.firstrun` styling) above the streak card, and the client `updateFirstRun(commitments)` refactored to drive BOTH banners from an inline `entryState` that mirrors the exported one (welcome-back shown iff there are words but none `active`/`paused`; first-run keeps its focus-once). No API/schema change (reads the existing `/api/commitments` load; `/me/` served directly from `me.js`, no `html.js` build). Tests: `me.test.js` (+5) — warm non-empty re-entry copy, the LAW scan over the re-entry strings (no shame incl. `miss`/`behind`, `\bAI\b`, clinical), `entryState` routing across `[]`/history/active/paused, the `#reentry` panel + copy render with the `class="card firstrun hidden"` default + `updateFirstRun`/`entryState` client wiring, and the copy folded into the design-LAW surface. 344/344 suite (+5); `eslint src` 0 errors; Node-driven render this session (re-entry card renders, entryState routes `[]`→first-word, `[kept,released]`/`[missed]`→welcome-back, `[kept,active]`/`[paused]`→in-flight, LAW-clean). **Live curl pending** (egress-restricted session; a founder browser spot-check on `/me/` — sign in as someone with kept words but nothing active → the "Welcome back." door — closes it) |
| R-231 | Per-word **momentum sparkline** in the R-222 detail panel (a returning "how am I doing on THIS one?" answered as a shape, not just a count): the per-word detail view (R-222) showed a kept timeline + a lifetime count but no *rhythm*, while `/me/` (R-229) and the coach view (R-228) already visualize momentum for the whole person. Now `GET /api/commitments/:id/detail` attaches a `momentum` block scoped to THIS word — a per-day count of the times it was KEPT over a trailing 14-day window in the word's own timezone — reusing the exact shared `momentum.js` engine, and the `/me/` detail panel renders it as a small bar sparkline ("Momentum on this word") above the kept timeline, with a first-person summary ("You kept this word N times over the last 14 days. Your best day: M."). Momentum-only **by construction and by the design LAW**: built from `status='kept'` instants ONLY, so a quiet day is a short/grey bar — the *absence* of a win, never a miss — and a never-yet-kept word shows a flat baseline reading as a clean stretch, never a chart of nothing (rendered only once the word has a kept check-in). One tested sparkline shape now serves all three surfaces without sharing a voice | building | `accountability.js`: new per-word copy `detailMomentumHeadingCopy()`/`detailMomentumIntroCopy()`/`detailMomentumSummaryCopy({total,days,peak})` (folded into `meCopySurface()` so the design-LAW gate scans them), and the `/api/commitments/:id/detail` handler now feeds the already-fetched kept timestamps (`kept.map(k=>k.kept_at)`, the 50-most-recent window that fully covers 14 days) + the commitment's own timezone into the shared `buildMomentum({...,intro,summary})`, attached as `momentum` (no new DB query, no schema change). `me.js`: extracted a shared client `sparkBlockHTML(m)` + `hasMomentum(m)` used by BOTH `renderMomentum` (the R-229 card, refactored to reuse them — byte-identical output) and `renderDetail` (which now renders the per-word sparkline under a `DETAIL_MOMENTUM_HEADING`, shown only when `hasMomentum(d.momentum)`); the three new copy fns imported + added to `meCopySurface()`. Tests: `detail.test.js` (+4) drives the real route through itty-router + a D1 double — asserts the attached `momentum` block is a 14-day kept-only axis with a 14-glyph sparkline, the total/peak, the word's own timezone, empty-safe flat baseline ("clean stretch") for a never-kept word, and **no `missed` query**; the detail copy-LAW scan (+the new heading/intro/summary samples) stays clean (no shame incl. `miss`/`behind`, `\bAI\b`, clinical). 348/348 suite (+4); `eslint src` 0 errors (no new warnings on touched files); Node-driven end-to-end render this session (NY-tz busy word → scaled sparkline `▁…▅▁█▅▁`, peak-2 day, first-person summary; quiet word → flat baseline + "clean stretch"; LAW-clean). **Live curl pending** (egress-restricted session; a founder browser spot-check — open a word's "View" panel on `/me/` for a word kept a few times → the "Momentum on this word" sparkline above its kept timeline — closes it) |
| R-232 | Warm **"best day"** callout on a word's momentum peak (name WHICH day it peaked, not just how tall the bar was): the per-word detail sparkline (R-231) drew the shape and its summary named the peak *count* ("Your best day: M"), but never *which* day that was — the momentum engine already computed `peak.date` and it went unused. Now the per-word detail panel adds a warm one-liner under the sparkline — "Your best day on this word so far: {day} — {N} kept. 🔥" — where `{day}` is a friendly relative phrase (`today`/`yesterday`/a plain weekday within the last week/`Weekday, Mon D` once a week or more has passed, so it can never be mistaken for this week's same weekday), resolved in the word's own timezone. Anti-shame **by construction**: shown ONLY for a genuine standout (a day with **2+** kept) so a word of all-single days gets no arbitrary "best day", and a quiet/never-kept word shows nothing; the copy celebrates a high point and never sets it against now — "so far" frames it as a mark still open to being beaten, never "you were better before" | building | `momentum.js`: new pure `describePeakDay(dayLabel,{nowISO,timezone})` — DST-agnostic whole-day distance by calendar-label math + Intl-free weekday/month naming, returns `''` for a missing/malformed label. `accountability.js`: new `detailPeakDayCopy({count,whenPhrase})` (returns `''` below the 2-kept threshold or with no day; folded into `meCopySurface()` so the design-LAW gate scans it), and the `/api/commitments/:id/detail` handler now anchors one `nowISO`, passes it to `buildMomentum` (deterministic bucketing) and to `describePeakDay`, and attaches the result as `momentum.peakDay` (no new DB query, no schema change). `me.js`: `sparkBlockHTML(m)` appends a `.momentum-peak` line when `m.peakDay` is set — undefined on the `/me/` and coach momentum payloads, so ONE renderer still serves every surface and only the detail panel shows the callout; `.momentum-peak` style + the copy fn imported/added to `meCopySurface()`. Tests: `detail.test.js` (+11) — the endpoint attaches a non-empty `peakDay` naming the count for a 2-kept standout, `''` when every kept day is a single (peak 1) and `''` for a never-kept word; `describePeakDay` unit sweep (today/yesterday/weekday-within-a-week/date-pinned-past-a-week/tz day boundary/malformed→`''`); `detailPeakDayCopy` celebrates a standout with "so far" framing and is silent below threshold; the detail copy-LAW scan (+peak samples) stays clean (no shame incl. `miss`/`behind`, `\bAI\b`, clinical). 357/357 suite (+9); `eslint src` 0 errors (no new warnings on touched files); Node-driven end-to-end render this session (NY-tz busy word → "Your best day on this word so far: Friday — 3 kept. 🔥"; all-singles word → no callout; quiet word → no callout; phrasing sweep today→"Monday, Jun 29"; LAW-clean). **Live curl pending** (egress-restricted session; a founder browser spot-check — open the "View" panel on a word kept 2+ times in one day → the "best day" line under its momentum sparkline — closes it) |
| R-233 | Person-facing **next check-in on the `/me/` list** (the concrete moment the bro next shows up on every word, at a glance): the commitment list card showed each word's original start time + cadence, but the *next* check-in moment lived only inside the per-word detail panel (R-222) — you had to open every card one at a time to answer "when does the bro come back on this one?". Now `GET /api/commitments` attaches a `next_checkin` to each **active** word (the soonest still-outstanding check-in), and `/me/` renders it right on the card — the person-side twin of the coach's next-check-in (R-224). Anti-shame **by construction**: `next_checkin` is only ever set for `active` words (a kept/moved/released word carries none), and when that moment has already **passed but the check-in is still open**, the UI shows a warm "Still here whenever you're ready" line — never "late", "overdue", or a miss. The waiting line is styled with a gentle amber accent, never red/alarm | building | `accountability.js`: the `/api/commitments` handler adds ONE grouped query (`SELECT commitment_id, MIN(scheduled_for) … WHERE status IN ('pending','sent','deferred') GROUP BY commitment_id` — no N+1) and maps it onto each row, setting `next_checkin` only for `status==='active'` (else null). No schema change (reads existing `commitment_checkins`). `me.js`: two new copy fns `listNextCheckinLabelCopy()` ("Next check-in") + `listNextCheckinWaitingCopy()` ("Still here whenever you're ready"), a client `nextCheckinLineHTML(c)` that shows the label+time for a future check-in and the warm waiting line when it's already past (client-clock compare), rendered inside the active card; `.when.next`/`.when.next.waiting` styles (amber, no red). Tests: new `next-checkin-list.test.js` (+7) — the list attaches next_checkin to active words from the grouped query, never to a non-active word (even given a stray outstanding row), null when nothing is queued, exactly ONE grouped query (no N+1), auth required; plus a design-LAW scan on both copy strings (no `late`/`overdue`/`miss`/`behind`/`AI`/clinical) and the warm-"still here" assertion. 364/364 suite (+7); `node --check` clean on both files; Node-driven `renderMePage()` render this session confirms the new line + waiting copy + amber style are present and the page is LAW-clean. **Live curl pending** (egress-restricted session; a founder browser spot-check — on `/me/`, an active word shows "Next check-in: …" on its card, and a word whose check-in time has passed but is unresolved shows "Still here whenever you're ready" — closes it) |
| R-244 | **Degraded-delivery SLO — /health flags a loop that ticks but can't deliver** (extends R-242; reliability-as-SLO, the #74 cron outage's next silent-failure class). R-242 gave the loop a LIVENESS signal (`cron:last_tick` → /health `stale` → the off-platform `heartbeat.yml` probe), so a total cron death can't run dead unnoticed. But a cron can tick fresh every minute while EVERY send fails (a bad D1 migration, Telnyx 500s, a push-key regression) — it would stamp a fresh `last_tick` and read perfectly healthy, the moat silently dead behind a green /health. This adds the CORRECTNESS signal: `scheduled()` records a rolling `cron:delivery_fail_streak` (bumped only on a real send/DB error — `runDueCheckins` `failed`/`retry`; a `deferred` quiet-hours hold or `skipped` no-consent/no-channel park is a HEALTHY state and never counts), and `/health` reports `delivery_degraded` once the streak hits 3 (~3 min of continuous send failure, filtering a single transient blip). `heartbeat.yml` now fails loudly on `delivery_degraded` as well as `stale` — a degraded signal nobody watches would repeat #74's exact "nothing watching" flaw. Also hardens the pass: `runEscalations` (the secondary SMS knock) now runs in an ISOLATED try/catch inside `scheduled()`, so an escalation bug can no longer abort the pass before the heartbeat and masquerade as a delivery outage. | building | `checkins-cron.js`: pure `isDeliveryFailingTick(summary)` (failed/retry only), `DELIVERY_DEGRADED_STREAK=3`, `CRON_HEALTH_KEYS`, `recordCronHealth(env,{nowISO,delivery,escalation})` (best-effort per KV key — a blip on one signal never masks another; returns the streak), `readCronHealth(env,{nowMs,staleSeconds})` (all stale/degraded math; missing signals read the SAFE value — stale + not-degraded). `index.js`: `/health` now delegates to `readCronHealth` and surfaces `{ last_tick, age_seconds, stale, threshold_seconds, fail_streak, delivery_degraded, degraded_streak_threshold, last_summary }` (R-242's four fields kept byte-compatible); `scheduled()` isolates escalations + calls `recordCronHealth`. `heartbeat.yml`: probe also `process.exit(1)`s on `body.cron.delivery_degraded`. Tests: `cron-health.test.js` (+12) — failing-tick classifier (deferred/skipped never trip it), streak bump/reset, safe no-op without KV, degraded threshold, SAFE-default on missing/garbage signals, and the real `/health` route surfacing `delivery_degraded` end-to-end (mirrors `heartbeat.test.js`). Full suite **457/457**; branch cov 80.57 ≥ 79 floor; `node --check` clean; 0 new lint warnings. **Live curl pending**: flips to `live` when deployed `/health` returns the `delivery_degraded`/`fail_streak` fields (`false`/`0` on a healthy tick) — a founder/browser curl this egress-restricted loop can't run. |
| R-243 | **The bridge carries the focus intention into `/me/`** (extends R-241). The pomodoro→word offer (R-241) dropped the person at a blank `/me/`; now the timer carries the “🎯 One Thing” intention (`#intentionInput`, ≤120 chars) into `/me/?task=…`, and `/me/` prefills the give-your-word title + shows a warm, gate-scanned carry-over note (“Carrying over what you just focused on…”) — so the most-earned moment lands one tap from a kept word, not on a blank page. DESIGN LAW upheld **by construction**: the title is filled only when empty (never auto-committed), and the time + the word are always the person’s to set. R-241’s styling + bridge **live-verified this session** (`/me/` now wears the dark-indigo shell; the homepage serves `offerWordBridge`). | building | `public/index.html`: `offerWordBridge()` reads `#intentionInput` and builds `bridgeHref = '/me/?task='+encodeURIComponent(intent.trim().slice(0,120))` (bare `/me/` fallback when no intention). `me.js`: new `carryOverNoteCopy()` (folded into `meCopySurface()` → copy-law scanned), a `#carryNote` `.next-step` note in the give-word card, and `PREFILL_TASK` (read once from `?task=` at the top so it survives the sign-in gate) applied in `enterApp()` via `applyPrefill()` (fills `#title` only when empty, reveals the note, focuses “When?”); cleared on a successful commit. `html.js` regenerated. Tests: `me.test.js` (+3 — surface fold, offer-not-demand copy-law incl. `\bAI\b`/clinical/`miss`, render wiring). Full suite **445/445**; `node --check` clean. **Live curl pending**: flips to `live` on deployed `html.js` grep (`bridgeHref` / `/me/?task=`) + `/me/?task=…` prefilling the title field. |
| R-242 | **Delivery-loop heartbeat + external monitor** — the `crontab→crons` outage (#74) let `scheduled()` run dead ~5 weeks with nothing watching; this is the observer it lacked. `scheduled()` stamps `cron:last_tick` (ISO) in KV on every successful pass (own try/catch — a KV blip never masks a real delivery error above it); `GET /health` stays 200 for Worker liveness but now returns a `cron` block `{ last_tick, age_seconds, stale, threshold_seconds }` where `stale` is authoritative and a missing tick reads **stale** (never healthy-by-default; threshold 600s). A new **off-platform** GitHub Actions workflow (`.github/workflows/heartbeat.yml`, `*/15 * * * *` + `workflow_dispatch`) probes `/health` and fails loudly (default workflow-failure email) when `cron.stale` — running on GitHub, not the Worker, so it catches a total Worker/cron death a self-check never could. | building | `index.js`: `/health` reads `env.KV_CACHE.get('cron:last_tick')`, computes `age_seconds`/`stale` against `CRON_STALE_SECONDS=600`; `scheduled()` adds a guarded `KV_CACHE.put('cron:last_tick', now)` after `runDueCheckins`+`runEscalations`. New `heartbeat.test.js` (+5): 200-always, stale-when-never-ticked, healthy-recent, stale-past-threshold, stale-unparseable. Full suite **445/445**; `node --check` clean. **Live curl pending**: flips to `live` on deployed `/health` returning the `cron` block with `stale:false` after a real tick, plus the first green scheduled `heartbeat.yml` run. |
| R-241 | **Resonance repair — one product, one skin, and the pomodoro→word bridge** (#76, Wingspan W-RES; from the 2026-07-13 Resonance Council pass). The moat pages (`/me/`, `/me/report`, `/coach/`) rendered as unstyled white-wireframe pages while the commodity timer wore the polished dark-indigo skin, and a completed WORK pomodoro — the app’s most-earned moment — offered no path into accountability, so the free→accountability conversion moment was structurally missing (the home app had only a buried footer link to `/me/`). Now a shared `page-shell.js` ports the home app’s **exact** design tokens (dark indigo, DM Sans) onto all three moat pages as one visual family; the home nav rail gains a persistent **“Your word” → `/me/`** entry; and on work-pomodoro completion a warm, dismissible, **once-per-session** offer (“Nice. Want me to hold you to the next one?”) links into `/me/`. Engine-independent (frontend only — Phase B/C stay gated). DESIGN LAW upheld **by construction**: an OFFER, never a nag — dismissible in one tap (“Not now”), shown at most once per app session (`__fbWordBridgeShown` latch), no tally; every existing copy string preserved. | building | New `api/src/page-shell.js` (`pageHead`/`pageNav`/`pageShellStyle`) consumed by `me.js` (`renderMePage`), `report.js` (`renderReportPage`), and the `/coach/` route in `index.js` — each page’s inline light-wireframe `<style>` replaced by the shared dark-indigo shell (net −141/+45 lines across the three). `public/index.html`: `offerWordBridge()` (guarded, non-fatal, once-per-session latch) called from `pomoComplete()` on a completed WORK interval; a `🤝 Your word` `.nav-item` anchor → `/me/` added to the primary nav rail; `api/src/html.js` regenerated via `create-html-module.js`. Tests: full suite **437/437** (copy-law battery green; `.spark-bar` selector preserved so momentum tests hold); `node --check` clean on all touched files + built `html.js`; `eslint` no new warnings on touched files. **Live curl pending** (egress-restricted session): flips to `live` on deployed-`focusbro-production`-bundle grep (`offerWordBridge` / `pageShellStyle` / the `navWord` anchor) + a founder browser check that `/`, `/me/`, `/me/report` read as one visual family and a completed pomodoro shows the one-time bridge linking into `/me/`. |
| R-234 | Coach-visible **next check-in on the roster list** (the concrete moment the bro next shows up for each client, at a glance): R-224 gave a coach the next check-in, but only INSIDE the per-client "View rhythm" drill-in — to answer "when does the bro next show up for them?" a coach had to open every client one at a time. Now `GET /api/coach/clients` attaches, to each **active** client, the soonest still-outstanding check-in across their active commitments (`next_checkin` ISO + a warm `next_checkin_line`), and the `/coach/` roster card renders it right under the client's status — the coach-side twin of the person's own next-check-in on `/me/` (R-233). Anti-shame **by construction and by the design LAW**: the grouped query JOINs each outstanding check-in to an **active** commitment (`status IN ('pending','sent','deferred')`, `MIN(scheduled_for)`) so a stray row on a released word never leaks in and only a moment about to be KEPT is ever shown; a client with nothing queued carries no line; and when the soonest moment has already **passed but is still open**, the line reads a warm "Still here whenever they're ready" (amber accent, never red), never "late"/"overdue"/a miss. Pending clients (pre-consent) carry no check-in data at all. Deepens the skeleton coach dashboard toward the Phase C white-label — still gated on `@latimer-woods-tech/operator` | building | `coach.js`: new pure helpers `rosterNextCheckinWaitingCopy()` ("Still here whenever they're ready" — third-person twin of `me.js` waiting copy) + `rosterNextCheckinLine({iso,timezone,nowISO})` (future → reuse `nextCheckinCopy` "Next up …"; already-past-but-open → the warm waiting line; null/invalid → `''`). The `GET /api/coach/clients` handler adds ONE grouped query over ALL active client IDs (`SELECT c.user_id, MIN(cc.scheduled_for), c.timezone FROM commitment_checkins cc JOIN commitments c … WHERE c.status='active' AND cc.status IN ('pending','sent','deferred') GROUP BY c.user_id` — no N+1 across the roster; bare `timezone` follows the MIN row so each moment formats in its own commitment's zone), skipped entirely when the roster has no active clients; attaches `next_checkin` + `next_checkin_line` to active entries only. No schema change (reads existing `commitment_checkins`). `/coach/` roster render adds a `.roster-next` line on the active card (amber `.roster-next.waiting` when the moment is already past, client-clock compare). Tests: new `coach-roster-next-checkin.test.js` (+13) — the route attaches next_checkin + a forward line to an active client with an outstanding check-in, leaves a nothing-queued client line-free (null + `''`), never touches a PENDING client, renders the warm waiting line for a passed-but-open moment, uses exactly ONE grouped query (no N+1) and SKIPS it with no active clients, auth required; `rosterNextCheckinLine` pure sweep (future/past/null/invalid); design-LAW scan on the waiting + rendered-future copy (no `late`/`overdue`/`miss`/`behind`/`AI`/clinical). 377/377 suite (+13); `eslint src` 0 errors (no new warnings on touched files); `node --check` clean; inline coach-page script re-parsed clean (vm). **Live curl pending** (egress-restricted session; a founder browser spot-check — on `/coach/`, each active client's card now shows "Next up …" (or "Still here whenever they're ready" when a moment has passed) without opening "View rhythm" — closes it) |
| R-240 | **Escalation ladder v1: push → ONE SMS** (Wingspan W1, 2026-07-13 ADHD wingspan; the verified market mechanic — "a reminder that escalates until you actually start" — bounded so it can never become a guilt engine). A push check-in an ADHD brain swipes away by reflex used to be the END of the bro's attempt; now, when a **delivered** push check-in stays quiet (unanswered, commitment still active) past `ESCALATION_DELAY_MIN` (15m), the cron knocks ONCE more over SMS with warm, forward copy (`escalationCopy` + the DONE/LATER reply hint, so the two-way loop stays discoverable). Bounded **by construction**: exactly one escalation per check-in ever (`escalated_at` one-shot latch, set whatever the outcome — never a retry storm); consent-gated like every text (TCPA gate — granted SMS consent IS the opt-in; quiet-hours deferral leaves the row eligible for a later tick, the ladder never wakes anyone); LAW-clean copy scanned by the copy-law battery. Engine-independent (D1 + Telnyx SMS only; Phase B voice stays gated — when the voice engine lands, the ladder's top rung becomes the call). | building | `api/src/checkins-cron.js`: `runEscalations(env,{now,limit})` — scan (`status='sent' AND channel='push' AND responded_at IS NULL AND escalated_at IS NULL AND delivered_at <= now-15m AND m.status='active'`), consent gate, `deliverText` reuse, one-shot latch, `checkin_escalated` event on send; wired into `scheduled()` after `runDueCheckins` AND into `POST /api/internal/run-checkins` (returns `{summary, escalations}`) so one verification curl exercises the whole ladder. `api/src/accountability.js`: `escalationCopy()` (ally + hype). `api/src/events.js`: `EVENTS.CHECKIN_ESCALATED`. Schema: `commitment_checkins.escalated_at` (guarded ALTER + fresh CREATE) + `idx_checkins_escalation(status, delivered_at)`. Tests: `checkins-cron.test.js` (+7 — scan shape, exact 15m cutoff param, happy-path SMS w/ copy + latch + event, no-consent latch w/o SMS, quiet-hours defer leaves row untouched, one-shot-even-on-failure, no-phone skip) + `escalationCopy` in the copy-law battery (both personas). Suite green; eslint 0 errors. Flips to `live` on: deployed-bundle grep (`runEscalations`) + one real quiet push check-in observed escalating to a single SMS on the founder's phone (or via the internal trigger + a consented test number). |
| R-239 | **Timer/session usage feeds the retention spine** (completes L1, `docs/IMPROVEMENT_PLAN.md`) — R-235/R-236 built the first-party retention spine (`analytics_events`) and the D1/D7 return cohort, but only the commitment lifecycle landed there, so the numbers measured only the small accountability cohort — not the free-tier timer users who are the top of funnel. The coach pitch and the voice-moat thesis ("a call retains where a swipe fails") need retention measured across *actual product usage*. This bridges the canonical free-tier usage signal — a completed focus session (`session_complete`) accepted at the `/events` ingest route — into `analytics_events` via `recordEvent`, so D1/D7 return + active/returning users now reflect timer usage too. Engine-independent — reads/writes only D1, no voice/operator dependency, so Phase B/C stay gated. Design LAW held: this is an internal positive record, no user-facing copy, kept-word RATE is unaffected (it reads commitment-types only). | building | `api/src/events.js`: `recordEvent` gains an optional `at` timestamp (new `normalizeEventTime` → SQLite `YYYY-MM-DD HH:MM:SS` UTC) so a session synced later lands on its **real** day, not the sync day; default path unchanged (`datetime('now')`). `api/src/extended-routes.js`: the `/events` ingestion route mirrors each newly-accepted `session_complete` into the spine — inside the `if (!existing)` dedup guard (no double-count), non-fatal (recordEvent swallows errors, can't break ingestion or the timer), preserving the client timestamp. Tests: `events.test.js` (+3, 19/19) — `at` binds a normalized `created_at` (4-param form, real day), an unparseable `at` degrades to server time (never drops the row), omitting `at` is backward-compatible. Full runnable suite green locally (the itty-router-import files fail only for the pre-existing missing-vendored-dep condition in the loop env — identical on pristine `main`; CI `npm ci` resolves it). `node --check` clean on both touched files. **Live curl pending** (egress-restricted session): flips to `live` on deployed-bundle evidence (grep the bridge in `focusbro-production`) + `curl -H "x-cron-key: $CRON_TRIGGER_KEY" https://focusbro.net/api/internal/metrics?since_days=7` showing session usage in `active_users`/`retention` after a focus session syncs. |
| R-238 | **Guide → tool deep-links** (L4, `docs/IMPROVEMENT_PLAN.md`) — the cheapest reader→user conversion. All 16 guide CTAs previously pointed at the bare app root (`/`), dropping a reader who clicked "Open the Pomodoro timer" onto a cold dashboard. Now every CTA deep-links to `/?tool=<id>` and the app opens the matching tool state on arrival (focus / breathing / movement / sleep / rest, mapped per guide topic). Each guide page also now carries schema.org **Article** JSON-LD + Open Graph + Twitter card meta (the SEO/share half of L4). Engine-independent — no voice/operator dependency, so Phase B/C stay gated. Design LAW held: CTA copy stays warm and forward ("Start a focus session"), never a tally; 0 bare-word "AI" on any guide page. | building | `api/src/guides/index.js`: 16 CTAs → `/?tool=<id>`; new exported `TOOL_DEEPLINK_IDS` registry; `renderGuidePage()` emits escaped OG/Twitter meta + `</`-escaped Article JSON-LD. `public/index.html`: defensive deep-link handler (`TOOL_DEEPLINKS` map + `initToolDeepLink`) reads `?tool=<id>` on load, opens the mapped tool, clears the param via `replaceState`; unknown id = no-op, every action try/caught, so a bad link can't break the app or timer. `api/src/html.js` regenerated via `create-html-module.js`. Tests: new `guides-deeplink.test.js` (10) — every CTA is a valid `/?tool=<id>` whose id the served app (`html.js`) handles, JSON-LD valid Article per guide, OG/Twitter present, meta escaping, anti-shame CTA-copy scan. **Full suite 418/418** (+10); `eslint src` 0 errors; `node --check` + `vm.Script` parse of all inline app scripts clean. **Live curl pending** (egress-restricted session): flips to `live` on deployed-`focusbro-production`-bundle evidence (grep `TOOL_DEEPLINKS` + `?tool=` CTA hrefs + `application/ld+json`) + a founder click-through from a live guide CTA into the app's Focus view. |
| R-237 | **Weekly report V0** — the coach-proof artifact and L2 keystone (`docs/IMPROVEMENT_PLAN.md`): turn the loop's kept-word signals (measured by R-235/R-236) into a per-user weekly summary a person can SEE at `/me/report` and SHARE with a coach as plain text. This is the concrete GTM surface Phase 3 (coach activation) gates on ("≥5 real coach-visible reports"). The report reads REAL data — words kept this trailing week, the current kept-word run + lifetime total, the 14-day momentum shape, the active rhythms with each one's forward next-up moment, and ONE tiny forward step — and exports via **Copy report**, **Share with coach** (mailto), and **Download (.txt)**. Engine-independent (reads only the existing accountability tables; no voice/operator dependency). DESIGN LAW upheld, by construction: every number reads KEPT-word signal ONLY (words kept this week, a kept-word run, kept-per-day momentum, outstanding future check-ins) — a quiet week is a fresh page, never a miss tally; the "next tiny step" is always forward and warm; there is no missed/skipped series anywhere in the module. | building | New pure module `api/src/report.js`: `buildWeeklyReport()` (no DB — buckets kept instants into the 7-day week count + best day, builds the 14-day momentum block, maps active rhythms to a forward next-up line, assembles warm kept-word-framed copy), `renderReportText()` (shareable plain text), plus `registerReportRoutes()` (`GET /api/me/report` → `{report, text}`; auth-gated; reads only `status='kept'` + outstanding `pending/sent/deferred` rows, never a miss series) and `renderReportPage()` (the `/me/report` reading surface with Copy/Share/Download). Wired in `index.js` (import + `registerReportRoutes` + `/me/report` page route); a **Weekly report** link added to the `/me/` nav (`me.js`). Tests: new `report.test.js` (15 cases) — kept-this-week math (window bounds, best day, quiet-week zero not NaN), rhythm mapping + forward next-up (never "overdue"), `renderReportText` carries headline/numbers/sparkline/next-step and collapses blank runs / degrades on null, and the copy-LAW battery (no shame word, no "AI", no clinical claim) over every copy path + the full rendered text. `npx vitest run report+momentum+coach+events` → 72/72; full-suite `itty-router`-import failures are the pre-existing missing-vendored-dep condition (verified identical on pristine `main` by stash; resolved by CI `npm ci`); `node --check` clean on `report.js`/`index.js`/`me.js`. No `public/index.html` change → no `build:html` needed. **Live curl pending** (egress-restricted session): flips to `live` on deployed-`focusbro-production`-bundle evidence (grep `buildWeeklyReport` / `/api/me/report` / `renderReportPage`) + a founder browser load of `/me/report` showing the report with working Copy/Share/Download. |
| R-236 | D1/D7 **return-cohort retention** — the "retention half" of L1 (`docs/IMPROVEMENT_PLAN.md`): the single number the coach pitch and the voice-moat thesis are both unprovable without ("a call retains where a swipe fails" is unmeasured until this exists). `computeReturnCohorts(env,{nowISO})` reads the same first-party `analytics_events` spine R-235 built and answers, per user, "did the bro get them to come back?" — a user's **cohort** is the UTC day of their first-ever event; they "returned by DN" if they have ANY event on a later UTC day within N days of that first day. **Rolling** return (not day-exact) is the honest choice for a small dogfood/early cohort — the retention question the pitch actually asks — and a user is only counted as *eligible* for a DN rate once N full days have elapsed since their first day, so a brand-new user can never drag the rate down before they've had the chance to return. Attached under `computeLoopMetrics().retention` (`{d1,d7:{eligible,returned,rate}, new_users_7d}`) and surfaced on the existing `GET /api/internal/metrics?since_days=N` response. Engine-independent (reads only the event store — measures commitment lifecycle now, timer/session events when those are wired; no voice/operator dependency). DESIGN LAW upheld: retention is a positive, internal aggregate — not returning is **never** a per-person tally or a shame surface, and the module emits no user-facing copy. Non-fatal by construction: a cohort-query failure falls back to the empty shape and can never take down the rest of the metrics summary or the flow it observes. | building | New `computeReturnCohorts()` in `api/src/events.js` (one `WITH firsts…rets` aggregate query over `analytics_events`; SQLite `date()` math on the bound UTC `now` day); `computeLoopMetrics()` enriched with a non-fatal `retention` sub-object; `GET /api/internal/metrics` returns it automatically. Tests: `events.test.js` (+7, 16 total) — rolling D1/D7 rate math from the aggregate row, UTC-day bind stability, null-not-NaN when no cohort is eligible, empty-shape without a DB / on a null row, retention attached in `computeLoopMetrics`, and retention-failure-is-non-fatal (core metrics still return). `npx vitest run` → `events.test.js` 16/16; full-suite failures are the pre-existing local-env D1/workers-pool timeouts (verified identical on pristine `main` by stash), resolved by CI `npm ci` + the workers pool; `node --check` clean. **Live curl pending** (egress-restricted session): flips to `live` on deployed-`focusbro-production`-bundle evidence (grep `computeReturnCohorts` / `new_users_7d`) + a founder `GET /api/internal/metrics` showing a `retention` block. |
| R-235 | Accountability retention instrumentation (the loop-proof spine): a first-party, **D1-native** event store — NOT the Neon-backed `@latimer-woods-tech/analytics` package, which FocusBro's stack forbids — that records the loop's real transitions so retention can be measured. `docs/IMPROVEMENT_PLAN.md` names this (L1) the binding constraint of the whole product: the voice-call moat is unjustified and the coach pitch unsellable until the loop is measured. `recordEvent(env,{userId,type,data})` writes `commitment_created` / `commitment_kept` / `commitment_reschedule` / `commitment_missed` / `commitment_released` / `checkin_delivered` into a single `analytics_events` table (the same table `billing.js` + `sync.js` already INSERT into — it was **never CREATE'd**, so those writes were silently failing; this creates it, fixing that latent bug). `computeLoopMetrics(env,{sinceDays})` reads them back into the founder/coach numbers (kept-word rate, reschedule rate, active + returning users, counts by type), exposed read-only at `GET /api/internal/metrics?since_days=N` (guarded by `CRON_TRIGGER_KEY`; 404s when unset). DESIGN LAW upheld: the module emits **no user-facing copy**; "reschedule" is a protected outcome in the denominator, never a miss score; `commitment_missed` is counted only so the kept-word RATE is honest and is never surfaced as a tally. Emit is **non-fatal by construction** (`recordEvent` swallows its own errors) so instrumentation can never break a resolve, a delivery, or the timer; `applyCheckinOutcome` is the single emit point covering BOTH the in-app resolve route and the inbound-SMS reply path. Engine-independent (no voice/operator dependency). | building | New leaf module `api/src/events.js` (`recordEvent`, `computeLoopMetrics`, `outcomeEvent`, `clampSinceDays`, `EVENTS`; zero imports → no cycle). `analytics_events` table + 2 indexes (`event_type,created_at` / `user_id,created_at`) added to `initializeDatabase()` in `index.js`. Emit calls: `POST /api/commitments` (created), `applyCheckinOutcome` (kept/reschedule/missed — both surfaces), `POST /api/commitments/:id/release` (released), `runDueCheckins` on `sent` (delivered). New `GET /api/internal/metrics` route. Tests: `events.test.js` (+9) — recordEvent SQL/params + serialization, non-fatal on a throwing insert, no-op without DB/type; `outcomeEvent` mapping; `clampSinceDays` bounds; `computeLoopMetrics` totals + honest kept/reschedule rates + null-on-empty (no divide-by-zero) + a reschedule-heavy window counted as protected, never failure. `npx vitest run` → all runnable suites green (208 local; the 16 `itty-router`-import files fail only for a missing vendored dep in the loop env, pre-existing on pristine `main`, resolved by CI `npm ci`); `node --check` clean on all touched files. **Live curl pending** (egress-restricted session): flips to `live` on deployed-`focusbro-production`-bundle evidence (grep for `computeLoopMetrics` / `analytics_events` / `/api/internal/metrics`) + a founder `GET /api/internal/metrics` with the cron key. |
| R-228 | Coach-visible kept-word **momentum sparkline** (feel a client's momentum at a glance, not just one streak number): the coach detail endpoint `GET /api/coach/clients/:clientId` now attaches a `momentum` block — a per-day count of words KEPT over a trailing 14-day window in the client's own timezone — and the `/coach/` "View rhythm" panel renders it as a little bar sparkline above the cadence rows, with a warm summary ("N words kept over the last 14 days. Their best day: M kept."). Momentum-only **by construction and by the design LAW**: the window is built from `status='kept'` check-ins ONLY, so a day with no kept word is simply a short/grey bar — the *absence* of a win, never the *presence* of a miss — and there is no missed/skipped series anywhere in the block; an all-quiet window is a flat baseline that reads as a clean page ("every window is a fresh start"), not an empty gap. Day boundaries are DST-correct (Intl-based local-calendar bucketing). Consent gate unchanged (only `active` clients, 404 otherwise). Deepens the skeleton coach dashboard (R-208) toward the Phase C white-label — still gated on `@latimer-woods-tech/operator` | building | New pure helpers in `coach.js`: `localDayInZone(iso,tz)` (Intl `en-CA`→`YYYY-MM-DD`, UTC fallback), `bucketKeptByDay({timestamps,days,nowISO,timezone})` (per-local-day counts, calendar-label axis walk, out-of-window ignored), `sparklineBars(counts)` (8-glyph ramp; all-zero → flat baseline, never blank), `momentumIntroCopy()`/`momentumSummaryCopy({total,days,peak})` (folded into the coach copy-law scan), and `buildMomentum(...)` assembling `{intro,days,timezone,buckets,total,peak,sparkline,summary}`. The detail route adds a representative-timezone lookup (most-recent commitment zone, UTC fallback) + one windowed `status='kept'` query (`responded_at >= cutoff`, LIMIT 1000) → `buildMomentum`, attached as `momentum`. `/coach/` `renderRhythm()` gains a `momentumHtml()` that renders scaled `.spark-bar` bars (quiet day = `.spark-bar.zero` grey, `title="date: N kept"`, `role="img"` aria-label = sparkline + summary) + `.spark`/`.momentum` styles. No schema change (reads existing `commitment_checkins`). Tests: `coach.test.js` (+16) — `localDayInZone` UTC/west/east/garbage, `bucketKeptByDay` axis length+order+same-day merge+out-of-window+LOCAL-vs-UTC bucketing, `sparklineBars` one-glyph-per-entry/flat-baseline/scaling/object-input/empty, `buildMomentum` totals+peak+sparkline+summary and the quiet-window clean-page path, plus the new copy folded into the design-LAW scan (no shame incl. `miss`/`behind`, `\bAI\b`, clinical). 325/325 suite (+16). **Node-driven render check this session** (`momentumHtml` against real `buildMomentum` payloads: busy window → 14 scaled bars, tallest at 100%, correct peak/summary; quiet → 14 grey baseline bars + clean-page copy; empty momentum → nothing; rendered HTML design-LAW-clean). **Live curl pending** (egress-restricted session; a founder browser spot-check of "View rhythm" on `/coach/` — the sparkline above the cadence — closes it) |

## Rejected (do-not-build guardrails)

| ID | Requirement | Status | Reason |
|---|---|---|---|
| R-900 | "Keep Teams/Slack green" presence-faking (synthetic mouse/keyboard/wheel activity, idle suppression) in any framing | rejected | Google "enabling dishonest behavior" policy — a root cause of the AdSense rejection; permanently out regardless of monetization |
| R-901 | Promoting the keystroke-injection companion script (Scroll Lock/F15 senders) on monetized pages | rejected | Same Google policy class; also reintroduces the R-900 surface via a side door |
| R-902 | Fabricated or unverifiable citations in guides (invented studies, misattributed researchers, fake statistics) | rejected | Destroys the "genuine content" thesis the entire AdSense strategy rests on; thin-but-honest beats thick-and-fake |
| R-903 | Resubmitting to AdSense before the guides are live AND indexed (~2–4 weeks post-ship) | rejected | Premature resubmission burns a review cycle on the same "low value content" verdict; the resubmission click is founder-only (no AdSense API) |
| R-904 | The word "AI" in any user-facing copy | rejected | LWT portfolio-wide brand rule |
| R-905 | Paid subscriptions / paywall (for now) | rejected | Pre-traffic paywall kills the organic-acquisition loop the ad thesis depends on; revisit only per VISION kill-signal review |
